using UnityEngine;
using System.Collections;

public class SpinSign : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    void ApplyDamagePlayer(float damage)
    {
        //iTweenEvent.GetEvent(this.gameObject, "SpinSign").Play();
    }
}
